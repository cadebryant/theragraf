# Copilot Instructions

## Project Guidelines
- Always format PowerShell commands as a single line (no line continuations with backticks).
- When providing az CLI commands with JSON body arguments, use single-quoted JSON strings (e.g. --body '{"key":"value"}') rather than backslash-escaped double quotes.